/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/CodeSight",
  assetPrefix: "/CodeSight/",
  images: { unoptimized: true },
};

module.exports = nextConfig;
